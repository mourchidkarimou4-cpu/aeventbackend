name: Keep backend awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: curl -sS -o /dev/null -w "%{http_code}\n" https://aeventbackend.onrender.com/api/core/settings/ || true
